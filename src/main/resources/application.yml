# mysql
spring:
  datasource:
    #mysql配置
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/webapp
    username: root
    password: BitSheJianDianPing.2023 # 采用部署到本地的时候自己的密码

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.BITSheJianDianPing.bean

server:
  port: 8080
